# Environment configuration for Quiz API
PORT={{ app_port }}

# CORS Configuration
ALLOWED_ORIGINS={{ allowed_origins }}

# Database configuration
DB_PATH={{ db_path }}

# Node environment
NODE_ENV={{ node_env }}

# Logging
LOG_LEVEL=info